[terragrunt]
version = "latest"

[terragrunt.remote_state]
backend = "s3"

[terragrunt.generate]
provider = true
backend = true

[terragrunt.remote_state.config]
bucket = ""
key = "${path_relative_to_include()}/terraform.tfstate"
region = "us-east-1"
encrypt = true
